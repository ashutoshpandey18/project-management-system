# BACKEND TESTS
cd backend
python -m pytest
python manage.py test

# FRONTEND TESTS
cd frontend
npm test
npm test -- --coverage
npm run test:watch

# RUN ALL TESTS (Both backend + frontend)
cd backend && python -m pytest && cd ../frontend && npm test